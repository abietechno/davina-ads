# Base URL for the API. Defaults to "/api" (same-origin), which is correct when
# deployed on Vercel (front + api in one project) or running `vercel dev` locally.
# Only set this if the API is hosted on a different origin.
VITE_API_URL=/api
